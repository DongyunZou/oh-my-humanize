# Workflow Demos

These artifacts demonstrate workflow-language shapes, TUI behavior, fixtures,
or seed-bound examples. They are not practical promoted workflows and do not
need to satisfy the long-running promotion gate.

Use `../experimental/` for practical candidates under validation, and the
workflow directory root only for formally promoted built-ins.
